{
  "name": "s3-web-view",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build src/main.ts --outdir dist --target browser && bun build src/search/worker.ts --outfile dist/search-worker.js --target browser --format iife",
    "dev": "bun build src/main.ts --outdir dist --target browser --watch",
    "dev:worker": "bun build src/search/worker.ts --outfile dist/search-worker.js --target browser --format iife --watch",
    "vendor": "cp node_modules/sql.js/dist/sql-wasm.js node_modules/sql.js/dist/sql-wasm.wasm dist/",
    "serve": "miniserve --index index.html --port 8080 .",
    "check": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "sql.js": "1.14.2",
    "typescript": "^5.6.0"
  }
}
